/* Koptelov Nikita, 10-1, 06.06.2024 */

export interface IArrayChanger {
  arr: Array<number>;
}

export class Filer {
  static writeToFile(fileName: string, data: string) {
    // fs.writeFile(fileName, data, () => {});
  }
}
